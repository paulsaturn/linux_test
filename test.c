/**
   @filename :
   test.c
**/
#include <stdio.h>
#include <stdlib.h>


int main(int argc, char **argv)
{
    //char *lable = NULL;
    char *lable = "hello world";
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");
    printf("%s\n", lable ? : "?");


    return 0;
    
}
